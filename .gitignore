# Environment & Dependencies
node_modules/
.env
.env.local
*.pem
*.key

# Build outputs (Gradle, Android, Maven)
build/
dist/
*.class
*.dex
*.apk
*.jar
target/
*.o
*.so
*.cs3~
.buildCache

# Gradle cache & artifacts
.gradle/
gradle.lock
.buildscache
.buildConfig
local.properties

# IDEs (JetBrains IntelliJ + VSCode)
.idea/
*.iml
*.iws
*.ipr
.vscode/
.vscode-insiders/
*.swp
*.swo
*~
.DS_Store

# Generated & Large Data (extracted at runtime)
/data/              # Extracted provider analysis data
/browserless/.       # npm dependencies (generated via setup)
/upstream/.          # Reference repository (cloned separately)
/reaver_patches/    # Generated patch artifacts

# Logs & Temp
*.log
*.pid
.tmp/
_temp/
*.tmp

# Specific runtime outputs (validation, reports)
/docs/reports/
/docs/verification/
!/docs/verification/provider_e2e_validation.json
analysis/logs/

# Node/npm
package-lock.json
.npmrc

# Telemetry & monitoring
telemetry.log
telemetry.json

# CI artifacts (store locally but not in git)
/artifacts/
.github/cache/

# Local tooling/cache
/.serena/
/docs/.vite/
